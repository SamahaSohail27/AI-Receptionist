# AI Medical Receptionist — Python Dependencies
# conda activate ai-receptionist
# pip install -r requirements.txt

# -----------------------------------------------------------------------
# Core framework
# -----------------------------------------------------------------------
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
python-multipart>=0.0.9  # for form data (OAuth2 password flow)

# -----------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------
sqlalchemy>=2.0.29
asyncpg>=0.29.0          # async PostgreSQL driver
alembic>=1.13.0
psycopg2-binary>=2.9.9   # Alembic offline mode fallback

# -----------------------------------------------------------------------
# Auth
# -----------------------------------------------------------------------
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt<5.0,>=4.0   # passlib 1.7.4 incompatible with bcrypt 5.x

# -----------------------------------------------------------------------
# Task queue
# -----------------------------------------------------------------------
celery>=5.4.0
redis>=5.0.4
aioredis>=2.0.1

# -----------------------------------------------------------------------
# Voice pipeline — SACRED: do not upgrade pipecat
# -----------------------------------------------------------------------
pipecat-ai==0.0.85
# pipecat 0.0.85 [silero] extra — Silero VAD analyzer needs onnxruntime.
onnxruntime>=1.20.1,<2
# pipecat 0.0.85 [webrtc] extra — SmallWebRTCTransport powers the browser
# test session's audio (bot voice over a native WebRTC media track). aiortc
# is the WebRTC stack; opencv-python is imported by pipecat's smallwebrtc
# transport module (video support) even when only audio is used. Pinned to
# match the AI-Clinical-Triage env where this path is proven.
aiortc==1.11.0
opencv-python>=4.11,<5

# -----------------------------------------------------------------------
# STT providers
# -----------------------------------------------------------------------
# pipecat 0.0.85 declares `deepgram-sdk~=4.7.0`; pinning here so a fresh
# env install doesn't pull deepgram-sdk 6.x (whose API breaks pipecat's
# import of AsyncListenWebSocketClient at module load).
deepgram-sdk~=4.7.0
groq>=0.9.0
openai>=1.30.0
azure-cognitiveservices-speech>=1.38.0
google-cloud-speech>=2.26.0
assemblyai>=0.26.0

# -----------------------------------------------------------------------
# LLM providers
# -----------------------------------------------------------------------
# openai already listed above
anthropic>=0.28.0
google-generativeai>=0.7.0
# groq already listed above

# -----------------------------------------------------------------------
# TTS providers
# -----------------------------------------------------------------------
# azure-cognitiveservices-speech already listed above
# openai already listed above
elevenlabs>=1.2.0
google-cloud-texttospeech>=2.16.0
cartesia>=1.0.0

# -----------------------------------------------------------------------
# Telephony
# -----------------------------------------------------------------------
aiohttp>=3.9.5            # async HTTP for Plivo/Twilio/Telnyx REST APIs
# plivo SDK optional — using REST directly via aiohttp

# -----------------------------------------------------------------------
# Utilities
# -----------------------------------------------------------------------
pytz>=2024.1              # always pytz.timezone("Asia/Karachi"), never timedelta
jinja2>=3.1.4
aiofiles>=23.2.1
httpx>=0.27.0
python-dateutil>=2.9.0
